{
  //

  const sumArray = (arrNums: number[]): number => {
    return arrNums.reduce(
      (previousVal, presentVal) => previousVal + presentVal,
      0
    );
  };

  // const returnValue = sumArray([1, 2, 3, 4, 5]);
  // console.log(returnValue);

  //
}
